{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack expense tracker — Express + MySQL backend, React (Vite) frontend.",
  "scripts": {
    "dev": "concurrently -n server,client -c blueBright,magentaBright \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "db:init": "npm run db:init --prefix server",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
